# Copyright 2026 Retail Cortex
#
# Cloud Build pipeline for packaging AI Daily Brief MCP Server container image

steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '$_IMAGE_URL'
      - '-f'
      - 'deployments/mcp/Dockerfile'
      - '.'

images:
  - '$_IMAGE_URL'
